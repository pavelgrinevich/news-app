import { Component } from '@angular/core';

@Component({
  selector: 'body',
  template:
    `<header>load...</header>
    <main>load...</main>
    <aside>load...</aside>`
})

export class AppComponent { }
